# Maintenance

Evidence updates use `data/sources.json` and `data/pax-silica.json`.

Validation:

`python scripts/check_repo.py`

Generated public output is reviewed before publication and is not edited directly.

The public site is a static reviewed snapshot. Scheduled freshness checks are read-only.
